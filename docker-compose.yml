version: '3.8'

services: 
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development 

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: secret 
    ports:
      - "5432:5432"
